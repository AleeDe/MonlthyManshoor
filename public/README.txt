Place your logo image file here as `logo.png`.

Vite serves files from /public at the root URL. After adding, it will be available at /logo.png.
Recommended size/aspect: transparent PNG, landscape orientation. The UI uses responsive heights so any larger source will downscale cleanly.
